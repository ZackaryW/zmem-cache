[project]
name = "zmem-cache-bdd"
version = "1.0.1"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "behave>=1.3.0",
    "ruff>=0.12.0",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
target-version = "py314"

## 1. Public Behavior Contract

- [x] 1.1 Extend the independently runnable command-attention, repository-indexing, and commit-checking Behave roots for default bounds, whole-commit node selection, bounded anchors, unlimited overrides, and deep proposed effects.
- [x] 1.2 Run all affected feature roots fail-first and record the missing request, selection, metadata, and incomplete-history behavior.

## 2. Canonical Attention Utilities

- [x] 2.1 Add core/service unit case matrices for limit validation, environment/request precedence, bounded newest-first Git discovery, whole-commit node selection, proposed-node reservation, and selection reporting.
- [x] 2.2 Implement typed attention policy and selection utilities plus parser-only host inspection, preserving chronological expansion of selected commits.
- [x] 2.3 Add store/schema coverage and implementation for attention-view anchor identity, stale bounded-row removal, compatible fast-forwarding, and transactional bounded rebuilds.

## 3. Service Composition

- [x] 3.1 Extend native CLI and daemon requests with optional commit/node limits and structured effective-policy, usage, truncation, and reached-bound output.
- [x] 3.2 Apply canonical selection to add/query synchronization, rebuilds, and deep proposed/reference checks without partial commits or persistent preview state.
- [x] 3.3 Upgrade protocol/schema and documentation while preserving concurrency, retention, extension trust, and hook-suppression behavior.

## 4. Verification and Reconciliation

- [x] 4.1 Run focused crate tests and all affected Behave roots GREEN.
- [x] 4.2 Run Cargo lock, supported toolchain, format, strict Clippy, complete workspace tests, independently runnable Behave roots, Python gates, and release build.
- [x] 4.3 Reconcile mature behavior into canonical command-attention, repository-indexing, and commit-checking specs and validate OpenSpec strictly.
